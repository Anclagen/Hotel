<!DOCTYPE html>
<html>
  <head>
    <%- include('./partials/head.ejs') %>
  </head>
  <body>
    <%- include('./partials/nav.ejs') %>

    <h2 class="text-center text-primary"><i><%=user.FirstName%> <%=user.LastName%></i></h2>
    <h3 class="p-3">My reservations:</h2>
    <table class="table">
      <thead>
        <tr>
          <th scope="col">Hotel name</th>
          <th scope="col">Hotel location</th>
          <th scope="col">Room capacity</th>
          <th scope="col">Price per day</th>
          <th scope="col">Rent from:</th>
          <th scope="col">Rent to:</th>
        </tr>
      </thead>
      <tbody>
          <% user.Rooms.forEach(function(room){ %>
              <tr>
                  <td><%=room.Hotel.Name%></td>
                  <td><%=room.Hotel.Location%></td>
                  <td><%=room.Capacity%></td>
                  <td><%=room.Price%></td>
                  <td><%=new Date(room.Reservations.StartDate).toLocaleDateString()%></td>
                  <td><%=new Date(room.Reservations.EndDate).toLocaleDateString()%></td>
              </tr>
          <% });%>
      </tbody>
    </table>
  </body>
</html>